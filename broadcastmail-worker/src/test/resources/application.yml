spring:
  datasource:
    url: jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:test}
    username: ${PGUSER:postgres}
    password: ${PGPASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  encryption:
    key: 01234567890123456789012345678901
  frontend:
    url: http://localhost:3000

supabase:
  oauth:
    client-id: test-client-id
    client-secret: test-client-secret
    redirect-uri: http://localhost:8080/api/v1/oauth/supabase/callback

test:
  supabase:
    jdbc-url: jdbc:postgresql://127.0.0.1:54322/postgres
    password: postgres
    project-ref: cxsfavxuutpwjwkqmrws
    role-password: testpassword123

